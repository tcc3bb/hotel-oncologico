Bom, agora vamos para a parte das reservas. O paciente vai poder reservar um quarto de hotel (a parte de cadastro de reservas já está feita e funcionando) e eu quero que ele tenha acesso a todas as reservas dele em uma seção do painel do paciente. As reservas são feitas em nome do ID do paciente que está logado, e elas são armazenadas na tabela Reserva.      

No EJS já tem um exemplo de como eu quero que fique mais ou menos a tabela de reservas. (No botão 'detalhes' ao ser clicado, deve exibir um modal com todas as informações do quarto escolhido, incluindo as fotos)



🔧Sempre que quiser atualizar o projeto:
    1. Editar localmente o código e o banco no Laragon.
    2. Testar tudo localmente (localhost).
    3. Exportar o banco local (hotel.sql) no HeidiSQL.
    4. Importar o arquivo no banco do Railway (via HeidiSQL).
    5. Salvar e enviar código:

        git add .                                                               
        git commit -m "Atualização do TCC"
        git push origin main

    6. Aguardar o Render fazer o deploy automático.
    7. Ver o site online atualizado.

EXEMPLO DE CÓDIGOS DO TERMINAL:
    git add app/infra/connectionFactory.js
    git commit -m "Corrige connectionFactory removendo connectMYSQL"
    git push origin main


KEY (Nome da Variável)      VALUE (Valor)
DB_HOST                     switchback.proxy.rlwy.net
DB_PORT                     51074
DB_USER                     root
DB_PASS                     aMMHZfHXBatNlkjOObUGbMzyPNEXjFqn
DB_NAME                     hotel



var connectMYSQL = function(){
    return mysql.createConnection({
        host: 'localhost',
        database: 'hotel',
        user: 'root',
        password: ''
    });
}